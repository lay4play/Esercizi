#include <stdlib.h>
#include <stdio.h>
#include <string.h>

int main(){
	char sito[50], tmp[100];
	printf("Insert webpage name");
	scanf("%s", &sito);
	strcpy(tmp, "xdg-open https://");
	strcat(tmp, sito);
	strcat(tmp, ".com");
	system(tmp);
	return 0;
}
	
